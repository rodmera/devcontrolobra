<div class="container">
<p id="notice"><%= notice %></p>

<p>
  <strong>Razon social:</strong>
  <%= @project.mandante %>
</p>

<p>
  <strong>Alias:</strong>
  <%= @project.name %>
</p>


<%= link_to 'Editar', edit_project_path(@project), :class => 'btn btn-primary btn-sm' %> |
<%= link_to 'Volver', projects_path, :class => 'btn btn-primary btn-sm' %>
